fastapi==0.115.0
# 0.0.12 carries 8 distinct CVE families in the multipart parser that
# POST /api/resumes/upload feeds attacker-controlled bodies to — boundary
# deformation DoS, unbounded part headers, negative Content-Length memory
# exhaustion, Content-Disposition parameter smuggling. 0.0.31 fixes all of the
# advisories reported against the pinned version.
python-multipart==0.0.31
uvicorn[standard]==0.30.6
sqlalchemy==2.0.35
psycopg[binary]==3.2.1
alembic==1.13.2
pydantic==2.9.2
pydantic-settings==2.5.2
# 3.4.0 fixes CVE-2024-33663 (algorithm confusion via OpenSSH ECDSA keys) and
# CVE-2024-33664 (JWE decompression DoS). We pin algorithms=["HS256"] and never
# decode JWEs, so neither was reachable — but the JWT layer is the trust
# anchor, and an unreachable-by-construction bug in the trust anchor should not
# be depended upon.
python-jose[cryptography]==3.4.0
argon2-cffi==23.1.0
# 1.2.2 fixes CVE-2026-28684 (symlink-following in set_key). We only *read*
# .env at startup, so it was not reachable; upgraded regardless.
python-dotenv==1.2.2
email-validator==2.2.0
openai>=1.0.0,<2.0.0
mangum>=0.17.0,<1.0.0
python-docx>=1.1.0
pypdf>=4.0.0
